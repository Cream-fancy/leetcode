## 9. 回文数

```cpp
bool isPalindrome(int x) {
  string s = std::to_string(x);
  int sz = s.size(), len=(sz>>1);
  --sz;
  int flag = 1;
  for(int i=0; i<len; i++){
    if(s[i]!=s[sz-i]) flag=0;
  }
  return flag;
}
```
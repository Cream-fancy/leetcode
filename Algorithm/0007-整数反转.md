## 7. 整数反转

```cpp
int reverse(int x) {
  int sign=0;
  long long t=x;
  if(t<0) t=-t,sign=1;
  long long res=0;
  while(t) res=res*10+t%10, t/=10;
  if(sign) res=-res;
  return res>=(1<<31)&&res<=(~(1<<31)) ? res : 0;
}
```
## 27. 移除元素

```cpp
int removeElement(vector<int>& nums, int val) {
  int sz=nums.size();
  if(!sz) return 0;
  int r=0;
  for(int i=0; i<sz; i++){
    if(nums[i]!=val)
      nums[r++]=nums[i];
  }
  return r;
}
```